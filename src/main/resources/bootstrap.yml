server:
  port: 8100

spring:
  application:
    name: gateway-service
  profiles:
    active: local
  cloud:
    config:
      uri: http://localhost:8500

---
spring:
  profiles: local


---
spring:
  profiles: dev

---
spring:
  profiles: prod
