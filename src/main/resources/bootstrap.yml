server:
  port: 8100

spring:
  application:
    name: gateway-service
  profiles:
    active: local
  cloud:
    config:
      uri: http://localhost:8500

application:
  project:
    name: gateway-service

logging:
  path:
    root: ./logs